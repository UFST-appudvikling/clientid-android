<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="dk.ufst.clientid">
    <uses-permission android:name="android.permission.INTERNET" />
    <application>

        <provider
            android:name="dk.ufst.clientid.InitProvider"
            android:authorities="${applicationId}.InitProvider"
            android:exported="false" />
    </application>
</manifest>
